import 'package:flutter/material.dart';

class MatchStudiosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('MatchStudiosScreen')),
      body: Center(child: Text('MatchStudiosScreen Placeholder')),
    );
  }
}