import 'package:flutter/material.dart';

class LiveMatchesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('LiveMatchesScreen')),
      body: Center(child: Text('LiveMatchesScreen Placeholder')),
    );
  }
}